const express = require("express");
const fs = require("fs");
const youtubedl = require("youtube-dl");
const mongoose = require("mongoose");

module.exports = {
  express,
  fs,
  youtubedl,
  mongoose
};
